# Staff Tracker

Simple red-themed staff clock-in/out web app using Node.js, Express and SQLite.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Initialize DB (optional — server will auto-init on first run)

```bash
npm run init-db
```

3. Start server

```bash
npm start
```

Open http://localhost:3000 and use sample passcodes: `1111`, `2222`, `3333`.
